# 发布检查

本次为 PosterPilot 的独立公开发布整理，日期：2026-09-14。

## 变更范围

- 统一前后端品牌、包名、内部异常名、配置前缀、主题存储键和容器名。
- 保留现有生成、检索、ReAct、HITL、评测、字体与案例功能，不进行架构重写。
- 补齐前端 `@types/node` 开发依赖，使干净安装后可完成 TypeScript 构建。
- 保留原项目运行目录，公开副本不继承其数据库、密钥与历史提交。
- 仅保留经整理的规则卡；未确认再分发授权的 PDF 与 44 个提取文本块不随仓库公开。

## 验证结果

| 检查 | 结果 |
|---|---|
| API 单元测试 | 224 passed |
| DeepGaze 服务单元测试 | 5 passed |
| 前端测试 | 17 个测试文件，36 passed |
| 前端干净依赖安装 | `npm ci` 成功 |
| TypeScript + Vite 构建 | 成功 |
| 公开知识数据校验 | 4 个来源，11 条规则卡（6 条 approved），8 个检索样例；原文块为空 |
| 离线词法检索基线 | 8 个样例：Recall@3=1.000，MRR=1.000 |

Python 测试复用了本机已安装依赖的解释器，但显式将导入位置指向公开副本，并核对了 `app` 与项目根路径。不是重新安装整套 Python 依赖的独立环境验收。

检索结果仅代表这个小型离线词法测试集，不是在线向量检索、重排效果或通用业务效果指标。本次没有调用付费模型，也没有重新执行真实 GPU 推理。

## 已知问题

- API/DeepGaze 范围 Ruff 仍有 289 个既有风格/导入等问题。与原代码的相对路径、规则、行列比较一致，未因改名增加定位；本次没有扩大范围批量改写代码。不能将 `npm run verify` 描述为全绿。
- npm 完整依赖审计报告 4 项开发依赖告警（2 moderate、2 high）；生产依赖审计为 0。未为消除告警自动升级现有工具链。
- API 测试存在一项来自依赖的弃用提示。
- 启动脚本与默认正文中文字体主要面向 Windows；跨平台部署仍需环境适配。
- 模型名称、下载依赖与 GPU/CUDA 版本需由部署者确认；单元测试通过不保证账户具有指定模型访问权限。

## 数据与授权

不发布 `.env`、简历/头像、原始 PDF、PDF 提取全文、虚拟环境、旧实现归档、模型权重、数据库、运行记录和日志。演示海报、参考图片、开源标题字体为单独选取的发布资产；第三方来源与许可见根目录 `THIRD_PARTY_NOTICES.md`。
