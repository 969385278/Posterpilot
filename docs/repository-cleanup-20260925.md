# 仓库整理记录（2026-09-25）

## 本次处理

- 删除 `scripts/verify-project.ps1`：与 `scripts/verify.ps1` 重复，引用不存在的测试说明和旧虚拟环境路径；仓库中未发现调用方。统一入口是 `npm run verify`。
- 本地移除43个 Python/pytest/Ruff 等可重建缓存目录，共323个文件、2,371,950字节。
- 本地 `tmp/` 的254个预览和旧检查文件（16,830,580字节）已移到项目外归档，未永久销毁。恢复位置记录在本机 `work/repository-cleanup-20260925/cleanup-manifest.json`。
- 学习台网页目录中的6个自动截图/验收文件移至 `apps/learn/.runtime/archify-visual-check/`，更新验收说明，避免把开发截图作为正式网页资源上传。
- `.gitignore` 补充统一的 `.runtime/` 忽略，以及含个人信息/绝对路径的本机简历核验与完成记录忽略。
- README 补全整个目录树、模块职责、应用关系、实验入口及本地数据边界，修正默认路由/视觉适配器描述。
- `apps/execution-learner/README.md` 标记历史快照与当前生产源码存在差异。

## 为什么保留这些文件

- `work/` 约2.3 GiB：评测输入、冻结源码、模型响应、图片、失败和作废实验，属于可追溯证据。删除负结果会破坏评测完整性。
- 两个学习应用：分别提供课程/原函数实验与可视化执行回放，功能不同，不能仅凭名称相似判定废弃。
- `docs/demo/` 的旧图：仍有说明文件及来源链引用；保留演示和出处。
- 计划、旧验收文档：保留设计历史，README 明确其时间和适用范围。
- 虚拟环境、node_modules、构建目录：属于本地开发依赖或可能正在使用的产物，本次未移除。
- SQLite、WAL/SHM、运行锁、Chroma索引、任务图片、审核发布记录及 `.env`：不是无用文件，不清除。

## GitHub同步边界

清理前本地已有大量尚未提交的功能代码。本次同步保留这些已有实现和对应测试，不以恢复旧版的方式清理工作区；没有为了通过检查批量格式化生产源码。

核查当前 `origin/main` 和索引，没有发现已跟踪的环境密钥、数据库、`work/`、`tmp/`、node_modules 或 Python 缓存路径。重复验证脚本曾被跟踪，本次删除会同步到新提交。这里检查的是当前文件树，未重写或声称清除所有历史提交。

公开仓库包含源码、开发测试样例、来源明确的静态资源和文档；本地实验原始日志和个人简历材料继续留在忽略目录。外部读者的导航见 [实验记录说明](evaluation-evidence-map.md)。

## 本次验证

|检查|结果|
|---|---|
|后端 `pytest apps/api/tests -q`|419通过|
|正式前端 Vitest|24个文件、58项通过|
|正式前端 TypeScript/Vite构建|通过|
|Python学习台 unittest|13项通过|
|执行学习器时间线测试|8/9通过；源码快照指纹与当前 `executor.py` 不同|
|执行学习器构建|通过；存在大于500 kB的bundle提示|
|Ruff `apps/api scripts`|355项既有问题，未宣称全量静态检查通过|
|DeepGaze独立测试|当前 `.venv-deepgaze` 缺少pytest，未完成；主环境尝试也因缺少python-multipart无法收集测试|

执行学习器的问题不能通过只更新哈希隐藏，应重新采集并核对教学内容后再称为当前版本演示。
这些检查没有重新调用付费模型，也未重新生成简历效果数字。主产品回归通过不代表完整 `npm run verify` 全部通过；静态检查和DeepGaze环境仍有待维护。

本次本地操作清单和整理前Git差异保存在 `work/repository-cleanup-20260925/`。
