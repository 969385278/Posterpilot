# 实际验证记录

日期：2026-09-20（北京时间）。本地地址：http://127.0.0.1:8891/ 。使用打包后的静态 Vite preview 页面验收。

## 已通过

- `npm.cmd run build`：TypeScript 检查和 Vite 构建通过。构建有单块超过 500 KB 的体积提示（压缩后约 168 KB），没有构建错误。
- `npm.cmd test`：9 项通过。覆盖步骤边界、严格晚于当前帧的停点、跨帧检测、任意跳转时的状态与产物、全部源码引用/行号/哈希、图片哈希、恢复状态、工具顺序、已保存的隔离对照结果。
- `npm.cmd run test:browser`：真实 Chrome 中 20 项通过。完整机器可读结果见 `browser-verification.json`。
- 当前业务源码的相关 Pytest：22 项通过，耗时 3.39 秒。测试文件为 `test_hitl_executor.py`、`test_action_validator.py`、`test_action_executor.py`、`test_react_tools.py`。
- 离线采集脚本已实际执行；内置断言通过：恢复状态相等、修改前后均停在人审、主视觉哈希一致、图片提供者总调用数仍为 1、工具成功、形成 1 个轮次快照、反例两边均拒绝且调用方布局不变。
- `git diff -- apps/api` 为空；既有业务源码与入口没有改变。学习器新文件全部放在独立目录。

Pytest 实际命令（仓库根目录）：

```powershell
$env:PYTHONPATH = (Resolve-Path apps/api).Path
& F:/workspace/GazePoster/.venv/Scripts/python.exe -m pytest apps/api/tests/agent/test_hitl_executor.py apps/api/tests/poster/test_action_validator.py apps/api/tests/poster/test_action_executor.py apps/api/tests/agent/test_react_tools.py -q
```

## 浏览器验收范围

1. 首次打开为第 0 帧、暂停，显示初始 PNG。
2. 第一次下一步进入第一个业务步骤并准确停在 149 帧。
3. 从讲解停点继续，会到下一停点，不重复停在原帧。
4. 4 倍速下下一步、重播都准确停帧。
5. 上一步、目录跳转均暂停，海报对应当前位置。
6. 拖到工具执行开始时，源码、数据、说明、图片同步，不显示未来输出。
7. 重播从本步起点展示，不带入后续渲染产物。
8. 暂停时展开与复制 JSON，剪贴板中的实际字号为 106。
9. 源码复制内容正确；选择文字以及操作倍速框时不触发播放器快捷键。
10. 点击画布里的输入对象选中对应检查面板，保持暂停。
11. 对照独立显示两种异常，退出恢复原来的精确帧，保持暂停。
12. 连续模式跨过教学停点；手动暂停有效；下一步仍在业务边界暂停。
13. 快速连续点击下一步，最终只保留一个正确目标停点。
14. 4 倍速学习模式遍历剩余停点，没有跳漏。
15. 9 个目录项与源码/数据和讲解的步骤标识一致。
16. 收起目录、调整检查面板宽度不会启动播放。
17. 普通页面上的左右键按业务步骤切换。
18. 实际鼠标拖动进度条后暂停，检查面板同步。
19. 1920×1080、1366×900 下没有页面横向溢出，已查看截图。
20. 浏览器没有脚本错误；全过程只向本地学习器发出静态 GET 请求。唯一 fetch 是案例 JSON；没有模型/业务 API 请求。

截图保存在本地 `.runtime/initial.png`、`step-tool.png`、`comparison.png` 和 `desktop-*.png`。该目录为临时验收产物，已加入忽略规则。

## 尚未验证 / 明确不在本版内

- 未验证真实模型生成质量、真实用户满意度或宣传团队的生产流程。本案例使用测试替身。
- 未录制 FastAPI 路由、前端事件或 SSE；这条链路直接从执行器开始。
- 没有逐行运行探针；源码范围与检查点之间的内部调用顺序属于源码解释。
- 除字号校验外，其他“没有这项设计会怎样”只是明确标注的推断，没有运行删除保护逻辑的实验。
- 没有验证其他浏览器、移动设备或屏幕阅读器。宽屏与普通桌面 Chrome 已验收。
- 候选图片没有打包；仅有实际候选元数据和渲染调用记录。没有真实运行耗时。
- 没有部署云端、在线模型问答、账号系统、复杂配音或 MP4 导出。

学习器测试不会重新采集案例。只有明确单独执行 `scripts/capture_case.py` 才会在本工具的隔离目录产生新的业务执行记录。
